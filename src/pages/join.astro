---
import Base from "../layouts/Base.astro";

const perks = [
  "Access to workshops & slides",
  "Join project teams (beginner → advanced)",
  "Hackathons & collaborations",
  "Tooling & lab guidance",
  "Member-only WhatsApp channels",
];

const tiers = [
  { name: "Standard", price: "£TBD / year", cta: "Get Membership", href: "https://your-payment-or-form" },
  { name: "Supporter", price: "£TBD+", desc: "Support our events & gear", cta: "Become a Supporter", href: "https://your-payment-or-form" },
];
---
<Base title="Join — Robotics Society" description="Join Robotics Society membership: perks, pricing, and how to get involved.">
  <section class="mx-auto max-w-6xl px-4 py-12">
    <h1 class="text-3xl font-semibold tracking-tight">Join Us</h1>
    <p class="text-slate-600 mt-2">Become a member and build robots with us.</p>

    <!-- Perks -->
    <div class="mt-8 grid md:grid-cols-2 gap-6">
      <article class="rounded-2xl shadow p-6 bg-white/70 border border-slate-200">
        <h3 class="font-semibold">Membership perks</h3>
        <ul class="mt-3 grid grid-cols-1 gap-2 text-slate-700">
          {perks.map((p) => <li>• {p}</li>)}
        </ul>
        <div class="mt-4 flex gap-3">
          <a class="rounded-xl px-4 py-2 bg-indigo-600 text-white hover:bg-indigo-700" href="#tiers">View tiers</a>
          <a class="rounded-xl px-4 py-2 border border-slate-300 hover:border-slate-400" href="/projects">Explore projects</a>
        </div>
      </article>

      <article class="rounded-2xl shadow p-6 bg-white/70 border border-slate-200">
        <h3 class="font-semibold">Get started</h3>
        <ol class="mt-3 list-decimal list-inside text-slate-700 space-y-1">
          <li>Pick a membership tier.</li>
          <li>Join our WhatsApp community.</li>
          <li>RSVP to the next workshop.</li>
        </ol>
        <div class="mt-4 flex gap-3">
          <a class="rounded-xl px-4 py-2 bg-slate-900 text-white hover:bg-slate-800" href="https://chat.whatsapp.com/your_invite" target="_blank">Join WhatsApp</a>
          <a class="rounded-xl px-4 py-2 border border-slate-300 hover:border-slate-400" href="/events">See events</a>
        </div>
      </article>
    </div>

    <!-- Pricing / Tiers -->
    <div id="tiers" class="mt-12 grid md:grid-cols-2 gap-6">
      {tiers.map((t) => (
        <article class="rounded-2xl shadow-lg p-6 bg-white/70 border border-slate-200 hover:shadow-xl transition">
          <div class="flex items-baseline justify-between">
            <h3 class="font-semibold text-xl">{t.name}</h3>
            <div class="text-slate-500">{t.price}</div>
          </div>
          {t.desc && <p class="text-slate-600 mt-1">{t.desc}</p>}
          <a class="mt-4 inline-block rounded-xl px-4 py-2 bg-indigo-600 text-white hover:bg-indigo-700" href={t.href} target="_blank">
            {t.cta}
          </a>
        </article>
      ))}
    </div>

    <!-- FAQ（纯 HTML 交互，无需 JS） -->
    <div class="mt-12">
      <h2 class="text-2xl font-semibold">FAQ</h2>
      <div class="mt-4 space-y-3">
        <details class="rounded-xl border border-slate-200 bg-white/70 p-4">
          <summary class="cursor-pointer font-medium">Do I need prior experience?</summary>
          <p class="mt-2 text-slate-700">No. We run beginner workshops and pair you with mentors.</p>
        </details>
        <details class="rounded-xl border border-slate-200 bg-white/70 p-4">
          <summary class="cursor-pointer font-medium">How do I join a project team?</summary>
          <p class="mt-2 text-slate-700">Check the Projects page and click “Join”; we’ll onboard you in WhatsApp.</p>
        </details>
        <details class="rounded-xl border border-slate-200 bg-white/70 p-4">
          <summary class="cursor-pointer font-medium">Can non-EE/CS majors join?</summary>
          <p class="mt-2 text-slate-700">Absolutely. We welcome all departments.</p>
        </details>
      </div>
    </div>
  </section>
</Base>
